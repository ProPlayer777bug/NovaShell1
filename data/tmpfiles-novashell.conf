# Keep the XDG runtime dir for the desktop user alive across boots: WSL has no
# display-manager logins, so without this GTK/WebKit apps start but render a
# blank window.
d /run/user/1000 0700 aara aara -
